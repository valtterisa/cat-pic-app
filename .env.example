# Copy to .env and set real values. Do not commit .env.
# On Railway, Postgres / Redis / MongoDB provide DATABASE_URL, REDIS_URL, MONGODB_URI.

# Required – from Railway when you add Postgres, Redis, MongoDB (or set for local dev)
DATABASE_URL=postgres://user:password@host:5432/dbname
REDIS_URL=redis://default:password@host:6379
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/content?authSource=admin

# Required – set yourself (e.g. openssl rand -base64 32)
JWT_SECRET=set-a-secret-at-least-32-chars

# Optional – defaults shown
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000
# VITE_API_BASE_URL=http://localhost:3001
# BACKEND_PORT=3001
# FRONTEND_PORT=3000
# DB_POOL_MAX=20
